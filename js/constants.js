// Indicates the template
var EVENTS = 0;
var MULTIPLE_DE_OU_PE = 1;
var MULTIPLE_PERIODS_AND_FACILITIES = 2;

// It allows for each sheet to import unlimited orgunits (columns)
// unlimited periods on rows, and unlimited dataElements for eachperiod (also in rows)
// The dataElements must be the same for every period
var UNLIMITED_ORGUNITS_PERIODS_DATAELEMENTS = 3;

var MONTHLY_PERIOD = 0;
var WEEKLY_PERIOD = 1;
var YEARLY_PERIOD = 2;

// Indicates if the excel has the same or different type of sheets 
var DIFFERENT_MODEL_SHEETS = 1;
var SAME_MODEL_SHEETS = 2;

var ORG_UID_SCHEME = "UID";
var ORG_CODE_SCHEME = "CODE";

var MONTHLY_PERIOD = "MONTHLY";